# Finish the tested review-note correction

Start from the accepted and deployed Telegram delivery candidate
8217f5579d02c4feed7dcdcc24c8e01ff8d35a71. Its native gate passed3181 tests,
and Fable1658 independently upheld all four signed criteria. Do not repeat or
rewrite that completed review. This brief is committed before native dispatch.

## Implement only these two corrections

1. Apply the small source patch from29ace2918aaa910b3d59cde93e3261dbcd481912
   (parent8217f55): provider.ts, structured-output.ts, provider.test.ts and its
   explanatory document. It was tested locally with typecheck and190 focused
   tests and exercised in real Fable review1658 through an isolated helper.
   It is NOT installed in the deployed package yet. Preserve its small scope.
   The adapter must admit native-valid251-to500 ASCII-character notes. The
   existing native500 UTF-16-unit limit still rejects oversized astral text.
   Keep the flat root-object schema, rubric IDs, mixed-payload refusal,
   judgement/custody/evidence rules and existing same-session correction.
   Never clip text, soften a judgement or widen the native parser limit.
2. Correct docs/TELEGRAM_DELIVERY_CANDIDATE_2026-09-15.json and the corresponding
   foundation-document claim. The manually listed22 paths omit src/claim.ts:
   ba101e694cd94907a493c0ae3a5efa666477e977..8217f55 changed23 source/test paths.
   Regenerate the original delivery snapshot from that exact Git diff, include
   every src/ path and its original8217f55 bytes, and recompute sourceDigest
   using the declared sorted path + NUL + fileHash + LF algorithm. Clearly
   identify sourceHead8217f55 so it is a historical delivery snapshot, NOT a
   claim to cover this follow-up's provider change or a mutable current tree.
   Do not alter original committed snapshots, stored artifacts or verdicts.
   Use one small executable assertion against the actual diff/hashes to prevent
   another omission; reuse an existing appropriate test/helper if practical.
   No new evidence framework, database table or second authoritative manifest.

The machine-sealed review inventory already contains all25 delivery paths,
including claim.ts. This is a documentation correction, not fabricated missing
evidence. Retain1658's other notes/questions for the following chat/delivery
work; do not silently label them resolved. In particular: routine digest anchor
movement, replay when pairing identity changes, retry noise for unknown legacy
rows, legacy pending-count views and post-send button placement.

## Verification and handoff

Use existing Fable5.1 subscription for all phases. Affected typecheck/tests
only during implementation, then unchanged native full gate once. Do not
rerun the whole suite in the builder or root. Add the note boundary regression
to existing provider tests rather than a new overlapping suite. Keep all
native validation safeguards and existing skips unchanged.

Before completion, validate the proof with parseProof/proofSubmissionProblems.
Keep exact command/reference strings within the current contract. Do not
invent shorter aliases as executed commands or recast an old test run as new.
Let the machine attach its truthful pending final-gate receipt. Supply changed
provider source/tests, shared note guidance and the executable manifest check
to the first independent review. No screenshot/manual-only criterion needed.

Out of scope: live database writes/migration, data retention or cleanup, new
schema epochs, scheduler/retry frameworks, provider installs, permission/model
default changes, public publishing, live Telegram sends/pairing, new connectors,
new hard time limits, test deletion/waivers, and full conversation parity.
No production runtime modification during build. Deploy only after exact
candidate verification/review using the existing guarded update procedure.
