# Restore historical evidence checks in GitHub CI

The user asked to merge PR #9. Its exact head `fe3a97e404fb1207ad31c83ef74a40d70ade573e` passed the native final gate, but GitHub run 35156979897 exposed a checkout setup bug. Both Linux matrix jobs fail only `src/provider.test.ts`'s historical Telegram delivery manifest test: `git diff ba101e694cd94907a493c0ae3a5efa666477e977 8217f5579d02c4feed7dcdcc24c8e01ff8d35a71 -- src/` reports the base object missing. The default `actions/checkout@v5` depth is one. Both named commits are ancestors of the PR head and exist in the published history. The existing test checks 23 source paths, every hash and the digest; keep it intact.

Make a bounded CI configuration correction in `.github/workflows/ci.yml`: the `test` job's checkout must use `with: { fetch-depth: 0 }` so all four Ubuntu/macOS Node 22/24 full-suite jobs have the historical objects. A short comment should explain that the evidence-manifest test reads historical commits. The focused Windows and containment jobs do not run this historical test and need no checkout change. Do not change product code, test assertions, skips, workflow permissions, test commands, dependency versions, credentials, verification timeouts or provider settings.

Reproduce with a temporary shallow Git clone of the supplied base (using `file://` with `--depth 1 --single-branch` works offline). Run the existing `node scripts/delivery-manifest-check.mjs`: it must fail because the historical commit is absent. Fetch the clone's history with `git fetch --unshallow` and rerun the same script: all 23 paths, hashes and the aggregate digest must pass. Keep the clone outside the production worktree and database. The existing manifest test is the regression; do not add a duplicate configuration-assertion test.

Record concise before/after command evidence under `evidence/ci-history/` and describe the fix here. Typecheck and the existing focused manifest test are sufficient during implementation. The unchanged approved full command belongs to the native final machine gate once for this candidate; submit pending verification instead of running that full suite in the builder or independent reviewer. Do not add an agent time limit, change or waive the 600-second verification limit, or delete tests. No new UI evidence is needed: the application is unchanged.

The input branch starts at the exact verified PR head above; preserve it. The operator will publish the verified descendant to PR #9, observe GitHub's fresh checks, merge the exact passing head, and reconcile publication status. The builder must not push, merge, install, change publication grants, send Telegram messages or modify the live database.

## Fix applied (2026-09-16)

`.github/workflows/ci.yml`: the `test` job's `actions/checkout@v5` step now sets `with: { fetch-depth: 0 }`, with a comment naming the reason (`src/provider.test.ts` runs `scripts/delivery-manifest-check.mjs`, which reads `ba101e6..8217f55` from Git). Nothing else in the workflow changed: `linux-native-containment`, `windows-baseline` and `native-windows` keep the default depth-1 checkout because their focused test lists never run the manifest test. No product code, test, test command, dependency, timeout, permission or provider setting was touched.

### Reproduction and proof

A temporary `git clone --depth 1 --single-branch` of this branch over `file://` (outside the worktree and database) has one commit and neither `ba101e69…` nor `8217f557…`. The unchanged `node scripts/delivery-manifest-check.mjs` fails there with `fatal: bad object ba101e694cd94907a493c0ae3a5efa666477e977` (exit 1), the same failure GitHub run 35156979897 reported. After `git fetch --unshallow` (605 commits, both objects present) the same script prints `lists all 23 src/ paths changed in ba101e6..8217f55; every sha256 matches 8217f55 bytes and sourceDigest recomputes` (exit 0). Full transcript: `evidence/ci-history/shallow-clone-repro.txt`.

Implementation checks: `npm run typecheck` (exit 0), the existing focused manifest test `npx vitest run src/provider.test.ts -t "historical Telegram delivery manifest"` (1 passed), and a YAML parse confirming only the `test` job checkout carries `fetch-depth: 0` — see `evidence/ci-history/`. The existing manifest test remains the regression; no configuration-assertion test was added. The unchanged approved full command runs once at the native final gate for this candidate.

## Final gate (2026-09-16, fix-1)

GitHub run 35158288446 at this head (`7c7b605`) passes all eight checks, including the four full-suite jobs that now fetch complete history. The native final gate for run 1715, however, ended with `exit 124 · timed out` at the approved 600 000 ms limit: the serial suite alone takes roughly 510–572 s, and the log shows no failing test (the receipt's `spawn-failed` label is the timeout, not a launch fault). Nothing in scope could change that — the timeout, the command and the tests are all protected — so the attempt parked. The operator chose to raise the window: verification grant 4 approves the same full command (`npm run typecheck && npm test -- --run --reporter=dot --no-file-parallelism && npm run build`) for 900 s with the same setup-recovery binding, to be restored to 600 s after this recovery. The candidate is unchanged; this section and `evidence/ci-history/final-gate-window.txt` are the only additions. See that transcript for the receipt excerpt and the re-run implementation checks.
