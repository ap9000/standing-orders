# Shared native session controls

This prepared second release adds `session list/show/changes/start/send/stop/resume/recover`
to the existing browser session owner. Browser and CLI share one catalog, native
thread and saved history. Live operator generation and project admission, exact
revision/turn guards, update drain and durable request receipts govern control.
Unknown delivery requires inspection; repeating a key never sends another turn.
`session show` offers a brief from saved items, with explicit agent-report labels
and detailed activity on request. It adds no supervisor or memory rewrite.

The original task base is exact verified UI native output
`7ee7a28a39c03ec08295468bdb82521c7ca85542`. Builder 1907 passed the unchanged
full machine gate; reviewer 1908 finished with all four criteria upheld and the
builder proof is verified. This prerequisite does not approve or verify the
session candidate. Its own signed dispatch, native gate and independent review
remain required.

The applied delta is the scoped session preparation from
`037d809f7a97ee965e24f0d4f1f2bf771be875b8` to
`fa7eb9e24d888af8c1c1c27089c1ceb102b7b44f`. All production source, tests, build
scripts, package and compiler configuration are byte-identical to that tested
preparation. The UI base's six-image inventory and complete UI evidence are
retained unchanged; its screenshots describe the inherited UI, not a new session
journey. The verified native/web corrections and 44px recovery link remain intact.

`evidence/session-cli/checks.json` records current source hashes and the complete
compressed logs from source `a9e16e1e38edadb787cc8189bf48fe87c6193c9d`: typecheck,
the normal test-harness build, 525 affected checks across ten files, and 30
CLI/browser entry checks across four files with their existing default spend
ceilings. These checks were reused after byte equality was verified, not rerun or
relabeled as a fresh run. The full native gate remains separate.

The complete historical live-native report from
`b60ffa69572bd39b6624f2aed763a5926d080fa9` is committed under
`evidence/session-cli/historical/`. Its original source identities, wording and
lossless archive are preserved. Labels such as “current” inside that historical
report refer to its original build, not this candidate. Two real messages kept
one thread across CLI start, cookie follow-up, restart and CLI resume. Later
saved-state reads launched no provider. These are historical observations for
unchanged session modules; no new provider turn or browser capture was made.

No session task has been dispatched and no runtime or production database has
been changed. Prepared dispatch and review helpers remain in this worktree's
ignored `output/private-dispatch/` directory. They require the final committed
candidate, UI reviewer 1908, verified prerequisite proof, all upheld UI
criteria, and the unchanged approved full command. The operator will review and
execute them after reviewing the complete committed session candidate.

The unchanged full native gate, independent review and deployment remain pending.
Install the UI and worker together through the normal drain, backup, compatibility,
process-exit and health checks. Passing preparation checks is not deployment.
Events/watch, reviewed memory capture, shipping controls and uncertain-delivery
consent through the session CLI remain outside this release. Existing browser
review and approval controls remain authoritative. Coordinator credentials do not
gain native session access or operator approval authority.

## Readable evidence correction

Native build 1909 passed the unchanged full gate (3,833 tests passed, 23 existing
skips, typecheck and build). Reviewer 1910 upheld the behavior and authority
criteria but could not assess c4 because the reused compressed logs and historical
native archive were not available as text in its sealed review context. That
attempt remains preserved and is not presented as fully verified.

This corrective preparation adds exact decoded, hash-bound records at
`evidence/session-cli/checks.txt`, `evidence/session-cli/entrypoints.txt`, and
`evidence/session-cli/historical/native-journey.json`. Their bytes match the
existing compressed archive receipts, which remain committed. The native journey
is still historical. Source, tests, existing limits and acceptance criteria are
unchanged. The corrective task starts at the same verified UI result `7ee7a28`
and includes the complete session delta, followed by a fresh native gate and
independent review. No prior task base is moved.
