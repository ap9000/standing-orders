# Native skill-test report receipts

After the feature was merged in PR #10 and deployed as cdf7a51, real Claude Opus skill tests #1732 and #1733 read SKILL.md and its supporting checklist. Run #1733 applied feedback filed through the installed phone viewport form, preserving the same package version and saying publication is unconfirmed. Shared chat turn #21 found the disabled skill and returned one project-scoped Skills control. These are real agent/chat tests, not the synthetic UI fixture. No human acceptance was recorded.

The live report revealed a presentation bug: native scout reports finish as built/report-delivered without a builder handoff, while the earlier visual fixture used no-change plus a handoff. A successful report therefore showed Changes saved and a missing-build-handoff fallback. This patch handles the actual lifecycle, reads the verified report summary, avoids repeating the short summary, and puts the full long summary in the existing "What the agent reported" disclosure (the report article itself never repeats it). Damaged reports still expose their evidence errors. No dispatch, permissions, schema, test limits, or skill packages change.

The existing report/evidence test now uses the actual native run shape and asserts the result, task and chat receipts. A focused desktop/phone proof is in evidence/project-skills-report-copy. The broader 25-check Skills journey remains applicable; its fixture has been corrected to use the native report shape. Only the affected copy views were recaptured, not the whole journey.

Apply this prepared commit on the sealed source result cdf7a51. Reuse unchanged feature and real-agent evidence. Let the unchanged approved native full verification command run once at the final gate; do not duplicate it during building or independent review. Then independently review, publish, merge and update UI/worker together through the standard drain, backup, compatibility and health checks. Schema remains 65. Verify installed report #1733 reads Report saved, displays its verified summary and has no missing-build-handoff fallback. Preserve the original dirty user checkout.
