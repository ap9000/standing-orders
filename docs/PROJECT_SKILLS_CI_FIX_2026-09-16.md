# Keep exact database byte checks fast

PR 10's candidate 710eb03 passed its native gate (3,356 tests and unchanged full typecheck/test/build) and independent review 1727 upheld all four feature criteria. Seven of eight GitHub checks passed. The Windows Node 22 job timed out in migration-v56-recipes.test.ts at the unchanged 15,000 ms limit in both workflow attempts 1 and 2. The attempt links and retained-log hashes are in evidence/project-skills-ci/manifest.json.

The prepared fix changes only the three database file-buffer equality assertions to Buffer.equals followed by toBe(true). This compares length and every byte natively. The same three complete file snapshots are still checked. All historical-row, schema-version, integrity, foreign-key, missing-history, and missing-authority assertions remain. The original on-disk fixture and production migration are unchanged. No test, skip, limit, approval, permission, production code, schema, UI, or verification command changes.

Profiling found that fresh database setup plus upgrade took about 100 ms, while the original test took 4,933 ms locally due to deep matcher traversal of file buffers. The repaired test passes in 121 ms with typecheck passing. Logs are retained in evidence/project-skills-ci. The test was rerun with the original source only to measure the baseline, then the proven fix was restored. No in-memory fixture change is part of this patch.

Apply only this prepared commit atop verified candidate 710eb03. Keep the complete skills feature and prior repairs. Compare production files and compiled runtime against the source to confirm this is a test-only repair; reuse the existing exact synthetic UI evidence. The native final gate must run the unchanged full command once on the new candidate; do not duplicate it manually. The operator will update the existing PR only after exact verification and independent review, then merge after all eight GitHub checks pass. The running installation remains on schema 64 until the corrected release is verified and deployed through the normal backup/migration/health checks.
