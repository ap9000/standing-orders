# Package 5 — live pilot record

September 14, 2026. **Three pilots and the evidence repair have run; publication and the gaps below remain open.** Base `1a08e43688daeb35f8b4a1e26be2a4cf1c3b277f`; all eight hosted CI jobs for that base passed. Integration branch: `codex/workspace-5-real-work-pilots`, product candidate `14e65755258f0f262542d7e4541718090983c962`. Installed app and global provider/permission settings are unchanged.

| Pilot | Current result |
| --- | --- |
| Long-request revisions | Native 1559 + linked revision 1565 built and passed full gates. Opus upheld the CSS change; two inherited criteria remain cannot-tell in the child review. Task 2 fixed the remaining file-list overflow. |
| Consistent task status | Native 1569 (`673a004`) passed its full gate and Opus upheld all three criteria. Linked build **1572** fixed the real message overflow and passed its gate. Its review upholds fit/actions but cannot judge inherited status logic. The signed manual-review requirement remains unresolved. |
| Clear revision names | Native **1581** / `7c813e0` and linked **1584** / `14e6575` passed full gates. The linked candidate moves IDs into details. Opus upheld all three inherited criteria on its first response; real phone review-batch wrapping and sibling-title distinction remain polish gaps. |
| Evidence / reviewer handoffs | Repairs in **1577**, **1579** and **bfa520b** passed focused/full gates. Real linked review **1585** used the restored source/revision context and upheld all three criteria without a correction or cannot-tell. Historical failures remain recorded; no artifact cap, approval or custody check was weakened. |

## First task: request and admission

- The current-source console runs locally at `127.0.0.1:4186`, restricted to the Standing Orders repository and using the real database. This is not an installed-app deployment.
- Chat used the existing **Claude Opus subscription** setting. Mate turns **7 and 8** both failed at their first provider step, with no proposal or task created. The UI stopped displaying activity but did not retain a useful visible error, including after reload.
- The operator used the regular task form instead. Task: `workspace5-long-request-revisions-20260914`. That is a **manual workaround**, not an autonomous chat-to-result success.
- The task was reserved to repo-bound runner `workspace5-pilot-20260914` before approval. Exact scope: `6c1ad06765d856202fecfb364261fa94`. Five signed criteria cover legacy inheritance, consistent new-input validation, exactly-once feedback/revisions, recoverable forms and desktop/phone layout, and the native full gate.
- Actual approved route: **Astra for build/repair and any future planning; Opus for review**. Direct-build mode means no planning run was requested. Native run **1559** started on **Codex / gpt-6-astra**. The saved Full access setting and existing idle-progress policy were preserved; no new dollar cap or agent time limit.
- During preparation, changing other phase overrides caused the proposed build profile to resolve back to configured Opus. The local approval guard caught this. Explicit build/repair overrides restored Astra before signing; no agent spent under the unintended route.

## Findings retained for review

1. The builder reproduced `bad-goal` in both a plain-note and a line-annotation legacy revision. It also found that a rejected web scope edit loses the submitted text. These are within pilot 1's scope.
2. The actual running task at 390×844 has document scroll width **486px**. Overflow traces through task details and long recap/goal text. The initial screen repeats the running state and exposes a long stop explanation. Before images: `output/playwright/workspace5-status-before-phone.png` and `workspace5-status-before-desktop.png`. These are real task screenshots, not synthetic fixtures or stitched captures.
3. Read-only chat diagnosis: Claude's cached login is valid and a minimal request with the real tool catalog succeeds. Reconstructing the saved conversation returned a successful provider envelope but was rejected locally as **bad-tool-call**. The captured call had **2,039-byte valid JSON arguments**, within the schema's 2,048-character allowance, but normalized `{id,name,args}` measured **2,080 bytes**, exceeding the adapter's 2,048-byte whole-call cap. The schema and validator budget different objects. The original two response bodies were not retained, so this is a reproduction, not proof of their exact raw response. No host tools executed during diagnosis. A separate chat repair was offered to the user; it is not part of the signed pilot-1 scope.

No first-attempt success rate or product-wide score is claimed. Preserve initial failures and manual interventions when final results arrive. The [pilot plan](WORKSPACE_5_REAL_WORK_PILOT_2026-09-14.md) remains the acceptance contract; physical-device and installation gates remain open.

## First candidate

- Native candidate `554d6977e2596f3f9c4066202a927b9bb85d1f6f`, 20 changed files. Actual run stamp confirms `codex / gpt-6-astra`, **subscription**, no dollar cost recorded.
- Builder reports typecheck, 557 affected tests across 11 suites, a final 293-test web rerun after editor changes, and 47 existing desktop/390px journey assertions. Root inspected the implementation diff and desktop result, phone feedback, phone approval and rejected-draft screenshots. These boundary-case screenshots use **synthetic fixtures**, not a native child builder.
- The implementation keeps inherited terms behind source-verified `sealRevision` and runtime-private insertion. Newly authored task text shares one validator. Rejected forms retain their draft and options; the specific error is beside the editor. Full native verification and Opus review are still pending at this entry.
- The subsequent shorter status-task request succeeded in real chat with one proposal after two provider steps. That does **not** resolve the reproduced large tool-call rejection or missing error.
- Native gate passed for `554d697`: exact approved command, exit 0, 167 suites, **2,885 tests passed / 23 existing platform skips**, 300.77 seconds. Check artifact 562 has SHA-256 `c0c0461ea82ddb959047c920661703b9a919bf28928e4318a2fc52e26a7f30e9`; stored output is shortened (14,254 of 36,451 bytes), with command, exit and summary retained. No second full gate was run by root.
- Review request 16 was claimed immediately by the existing installed worker, `alexsmacstudio-localdomain`, as run **1560**, Claude **Opus**, subscription. Our one-off tick therefore dispatched nothing; no duplicate reviewer was opened. The task's build reservation does not reserve independent review. Global settings and that worker were not changed.
- A separate candidate-source console on `127.0.0.1:4187` shows the native result, Changes and Checks. The result view fits 390px, but its real task details still measure 486px document width; `.recap` is 277px wide with 435px scroll width. Root saved ordinary feedback and a genuine diff annotation at `src/serve.ts:17542`, asking for wrapping without truncation. These are one concrete layout finding with a technical anchor, not two independent bugs. Evidence: `workspace5-native-result-phone.png`, `workspace5-candidate-task-phone.png`, and `workspace5-native-mixed-feedback-{phone,desktop}.png` under `output/playwright/`.
- Review 1560 and its bounded response-correction child 1561 failed `reviewer-stale-evidence`: the installed parser rejects the candidate's `pending-verification` proof, so the isolated reviewer was shown no proof file, then ingestion correctly refused the missing proof binding. Read-only comparison confirmed **current parser accepts / installed parser rejects** the unchanged proof; its hash still matches. The failed response was not accepted as a successful review.
- Recovery is manual: root dispatched the normal native `reviewPass` from current code with authenticated operator request and our runner. First recovery run 1562 refused before provider start because the helper omitted the custom evidence root (it defaulted to `.standing-orders/evidence`, not this installation's `.config/standing-orders/evidence`). This operator error consumed one attempt but no provider spend. The helper now verifies all source artifacts at the explicit root before requesting the remaining allowed attempt. Run 1563 is the third and final allowed review attempt; no cap, custody check or approval was weakened.
- Task 2 was filed through its real chat proposal as `show-one-clear-task-status`, then reserved to the pilot runner. It remains unapproved while pilot 1 is reviewed.
- Run 1563 successfully accepted **16 comments / five upholding judgements**. Its first reply exceeded two criterion-note length caps; the existing same-session correction 1564 repaired that response and passed. No full verification was duplicated. The review is valid, while root's separate live phone finding still requires a revision.
- The actual result UI sealed root's ordinary note, line annotation, reviewer context and root triage (19 notes total) into `revise-workspace5-long-request-revisions-20260914-from-1`. Root triage limits implementation to the demonstrated phone overflow; positive reviewer notes are not additional work. Native title still says “19 annotations”, reinforcing pilot 3's need. Exact inherited goal/exclusions/paths/five criteria and Astra profile were inspected; child approval `4d8ae36321687b8ec2db6a90d3421417` is fresh. Run **1565** builds from source `554d697` on Astra. The old base named in the inherited goal is historical and was clarified with native steering note 77; no inherited terms were rewritten.
- Task 2's scope preparation first refused because CLI semicolon delimiters split semicolons inside the chat-authored criterion prose. Before approval, those punctuation marks were changed to periods, preserving all three requirements, IDs, evidence and meaning. Seven explicit paths include the existing fixture/proof script and one assessment. No agent started under an unintended scope.
- Task 3, `give-revisions-recognizable-names`, was successfully proposed by the real Opus chat in two steps and filed from its card. It is reserved but unapproved. Six bounded files cover the existing UI/revision tests, result fixture/proof script and one assessment. Its exact three criteria retain recognizable names, Unicode/repeated-revision limits, and unchanged retry/lineage behavior; semicolon punctuation was normalized before approval for the same CLI constraint. No new model-generated naming or ID/history rewrite is authorized.
- Root previewed run 1565's built assets against the real database on owned port 4186. Long goal/exclusion recaps now wrap (`clientWidth === scrollWidth === 277`), but the adjacent allowed-files paragraph still scrolls to 435px, keeping the expanded document at 486px. Closing Scope removes that overflow. This is an **open real-data gap**, not a fully clean phone sign-off. Native steering note 78 carries the exact long allowed path into task 2's already-signed long-content/no-overflow requirement. Its fixture must include that field as well as goal/exclusions. The old installed app remains untouched.
- Before task 2 approval, root captured all three real phone surfaces. Work: **Needs your approval / Review and approve**. Chat: **Needs your approval**, a separate **queued** label, then another **Plan ready / Review plan** card. Details: raw **queued** beside the title, then **Review plan**. This is the concrete inconsistency being repaired; screenshots `workspace5-status-{work,chat,approval}-before-phone.png` are under `output/playwright/`.
- Linked candidate `cab3e6a59819649a2207c2401717e81d668d8da6` changes four files, with one production CSS line. Parent goal prefix, exact exclusions, all five criteria and source build 1559 were rechecked through the read-only native store. Its brief artifact 567 contains the exact 19-note batch, SHA-256 `1eeb6b061437668ca0344b67be724b1fd1ee1eb3eefe5502a1f973cc8119a7e8`. Native full gate remains running at this entry.
- The linked candidate's native gate subsequently passed: exit 0, **2,885 passed / 23 existing skips**, 167 suites, 311.84 seconds, plus typecheck/build. Check artifact 582 SHA-256 `f8ab9d36080cb0c71f2470a63f7dc284e50b1303fe2b4b622c7a96aafde26abf` is shortened at storage. Inherited review context artifact 583 is complete, SHA-256 `da72f52ea90207aea333496bc897be0e2c9c35b8d77e6b1c201f40adffe1180f`. Current-code native Opus review request 19 was opened after verifying these artifacts; no installed-app review attempt was wasted on this candidate.
- The complete context inventory does **not** mean complete source coverage: all five inherited coverage entries report gaps. Whole-file capture excludes `src/store.ts` (1,112,852 bytes) and other large files at the existing 49,152-byte item cap; aggregate capture is also bounded. Prior-review support is marked invalid because relevant files could not be sealed and `allBytesVerify` rejects the already-shortened source check log. The latter message says bindings “no longer match”, although the recorded hashes still verify. The native machine verdict is verified from the fresh gate; independent Opus run 1566 is still pending. No cap was raised, evidence hidden, or review waived to get past this finding.
- Review **1566** completed on its first root attempt, with seven comments and five judgements: **c1/c4/c5 uphold; c2/c3 cannot-tell** because inherited CLI/chat/HTTP source files exceed the packet limits. The native verdict remains verified from fresh checks. This is **not** five fresh independent upholds, and is not reported as such. The production delta is one CSS rule; c2/c3 implementation is byte-unchanged from the independently reviewed parent, and the fresh full gate plus focused retry/validation tests pass. Root carries the review-coverage weakness as follow-up, without altering its caps or judgments. The actual remaining allowed-file overflow belongs to task 2. Task 2 is now approved and starting on the reviewed delta, not silently held forever or presented as fixing the review engine.
- Review 1566 required two same-session format corrections: child 1567 failed malformed-review; child 1568 succeeded. Root checked the **accepted stored criterion rows**, authored by 1568, confirming the two cannot-tell judgments above. This was automatic response-format repair, not a new root review or a passing first response. Task 2 is run **1569**, Astra, in its own native worktree.
- Root's in-progress pilot-2 preview on owned port 4187 confirms the actual expanded allowed-files paragraph now fits: 277px client/scroll width, 379px document at a 390px viewport. The running task has one status card and **Watch the build**, with Stop reachable; real Work and focused Chat agree on **Running now / Watch the build**. Screens: `workspace5-status-running-after-phone.png` and `workspace5-status-paths-after-phone.png`. These are provisional until matched to the final candidate.
- The same real chat has another demonstrated long-content gap: the earlier pilot-1 request contains an unbroken assessment path; its message paragraph is 278px wide but scrolls to 402px, making the document 469px at 390px. Screens: `workspace5-status-chat-overflow-phone.png` and `workspace5-status-long-message-before-phone.png`. The builder is still running; no concurrent product edit or claimed phone sign-off. Retain this as actual-result feedback if it remains in the final candidate.
- Desktop's compact task-list sidebar still renders raw `queued` for the running task, while the main panel/Work/Chat say Running now. Root inspected the actual anchor and screenshot `workspace5-status-approval-after-desktop.png`. This is a second bounded pilot-2 finding, not an invitation to redesign the sidebar; use the existing shared status there too if still present at final inspection.
- Pilot 2 produced `673a004279abe920c2da3f1141eb1ce4fbe992c4`, Astra/subscription. Its final simplicity pass removed the redundant sidebar state badges and misleading approval caption, resolving the sidebar finding before handoff. Builder reports typecheck, 308 affected tests and 188 assertions in the existing desktop/390px proof (eight screenshots). Root viewed final review/approval/failure screenshots; these are synthetic. Native full gate is running. A restarted candidate console still reproduces the actual long-message document width of 469px at 390px; keep that one issue for a genuine linked revision.
- Separate read-only observation: the project-switcher summary still says `0 live` while this task is running, because `projectPeek` counts raw task states. This aggregate counter is outside the current task-card delta and remains follow-up, not a claim that every project-wide display is unified. No store/summary engine change was added here.
- Pilot-2 native gate passed on `673a004`: typecheck/build, **2,887 passed / 23 existing skips**, 167 suites, 304.51 seconds. Check artifact 601 SHA-256 `2f2df5c5a215a62bc38233db8ba9fe1b8ce98aff1cc5bd49047edb814e38fff7`, shortened at storage. Current-code native Opus review is run **1570**, request 20, first attempt. The signed `one-action` criterion explicitly includes manual-review evidence, so the machine does not silently turn its full-gate pass into complete acceptance.
- Root saved actual wrapping feedback as note 138, then added a correction: the overflowing chat message contains `docs/assessments/WORKSPACE_5_REAL_WORK_PILOT_2026-09-14.md`, not the separate `LONG_REQUEST_RESULT` allowed-files path. The DOM confirms this exact message and the same 469px document width. Both notes remain in native history. A temporary, initially empty chat draft survived opening the real result, Changes/Checks, saving the feedback and returning to chat; no model message was sent.
- Opus 1570 accepted 15 comments and three upholding judgments after one automatic same-session format correction, 1571. Root inspected the **accepted** rows, not only the initial response. The machine verdict remains **short** because `one-action` requires manual-review evidence; no exception was accepted. Review notes identify trade-offs, not fifteen required code changes: detail links open the scope/recovery anchors, `data-open-result` has no client interceptor, and the observed 72-row Work response took about 184ms (a sanity observation, not a scalability benchmark). Holding live refresh while a disclosure is open preserves reading/focus but can leave its status stale; retain that explicit trade-off for follow-up. Actual chat wrapping remains the bounded linked repair.
- Native result feedback (18 notes, including root's correction/triage and reviewer context) created `revise-show-one-clear-task-status-from-18-annotations-on`. The builder is asked only to fix the confirmed message overflow; no speculative review suggestions or redundant cleanup. Root compared exact inherited exclusions, paths, criteria and Astra profile, plus goal-prefix and fresh approval. Reserved to the pilot runner, approved digest **`dcae14a49d3178ff7a6b3a9b37ead494`**, then dispatched from `673a004`. No parent terms or acceptance were changed. The temporary chat draft was restored to its original empty state afterward.
- Linked build **1572** produced `3725ce1c27700774c2130939597980a1af6bf4af`: five files, one production CSS declaration (`overflow-wrap: anywhere` on `.thread .msg`). Builder reports typecheck, 308 affected tests, 196 assertions in the existing proof and ten synthetic viewport captures. Root independently opened its built assets against the real database: the actual message paragraph is now **278/278px**, with document/client **379/379px** at a 390px viewport; desktop document/client **1429/1429px** at 1440px. Screens `workspace5-status-long-message-after-{phone,desktop}.png` show the full text and path. Native full verification is running for this exact candidate.
- Root inspected the same candidate's real approval disclosure: full goal, exclusions, paths, criteria, Full access, exact Astra/Opus route, runtime details and password confirmation remain available. The phone **Approve & start** action is unbroken and reachable. No password or acceptance was submitted during this read-only check. Screenshot: `workspace5-native-approval-terms-phone.png`.
- The linked wrapping candidate's native gate passed: **2,887 passed / 23 existing skips**, 167 suites, 301.39 seconds, plus typecheck/build. Check artifact 619 SHA-256 `ccbede81ac1b8019adf862f33daf89e65affa87854c979e40ddabe685e07e922`, shortened at storage. Opus review is request 21 / run **1574**, first attempt. The tick correctly skipped the unapproved naming task; no naming provider started. Run 1573 is an unrelated installed-worker refusal for `dedupe-listings`, confirmed by its task reference, and is excluded from this cohort and left untouched.
- Opus 1574 required two existing same-session corrections (1575/1576), then accepted 11 comments: `one-action` and `fit` uphold, `agree` cannot-tell because inherited supporting files were absent. Valid JSON criterion notes measured 530/647/588 and 487/517/510 UTF-16 units against the native 500-unit limit. No review limit was relaxed. Native verdict remains short, including the unchanged manual-review requirement.
- The user then explicitly authorized fixing evidence and handoffs. Native task `workspace5-evidence-handoffs` was filed through the CLI, reserved to the owned pilot runner, scoped and freshly approved at digest `574f2532c1e358ea47b34ff936b05104`. Astra build **1577** starts from `3725ce1`; Opus is the approved reviewer. Scope covers integrity vs shortened capture, bounded large-file inherited context, and reviewer response limits. Naming remains unapproved until the repair is verified. No installed-service, old approval, history, cap, main or deployment changes.
- Root's read-only `workspace5-context-replay.mjs` reproduced the current run-1572 packet without storing/replacing evidence: 115,902 encoded bytes, valid roundtrip, all three coverage entries gaps, all prior `bindingsVerified` false despite intact shortened check artifact. `src/serve.ts` is 1,254,464 bytes versus the 49,152-byte whole-file limit. This is a diagnostic replay, not a new review or retroactive verdict.
- Official Claude structured-output documentation warns that string length constraints can be unsupported/removed by SDK conversion. Root saved native steering guidance to verify CLI behavior and preserve native strict parsing/corrections, not claim a schema-only guarantee. Root's first read-only DB query incorrectly imported absent `better-sqlite3`; corrected to the project's built-in `node:sqlite`, with no installation or data changes.
- Native 1577 committed **`f440f198a3d7c291b2955ac1c277a09cbfd61dc7`**, 13 files. Builder typecheck and 336 focused tests across seven existing suites passed. Root matched the assessed source diff exactly: 74,616 bytes, SHA-256 `a99f688936c1f356c447ac51e9ed3dcedbe0ba969fa1dfa3a860b81529f4ab6f`. Native full gate passed once: typecheck/build, **2,917 tests passed / 23 existing skips**, 167 suites, 314.60 seconds. Check artifact **630**, SHA-256 `5a7b26a2617bf62ee7fabc9bea84e295cdb6a135c2e3a54c6d6323c27911804e`, intentionally shortened, capture ok.
- Root replayed real run 1572 with the repaired code. The packet roundtrips and restores exact partial sections for serve (36,573 bytes), its tests (30,153), and the browser proof (33,355), including both source and revision segments. Prior evidence bindings now verify. Full-file coverage stays explicitly partial; one lower-priority unchanged test file exceeds the aggregate budget. This does not rewrite run 1572's accepted review or verdict.
- The run-1565 replay roundtrips with custody intact, but still excludes clean store/serve patches because source artifact **549** is globally marked redacted. Read-only inspection confirms the sole redaction marker is in `src/mate.test.ts`; store/serve sections have no secret-shaped lines or redaction markers. Root saved this real finding as a line annotation on run 1577, requesting exact clean per-file section reuse with all path/range/integrity checks and no recovery of hidden content. A linked follow-up will exercise the new native context format. No hidden bytes were read or exposed.
- Native Opus review **1578**, request 22 / first attempt, uses the repaired candidate's controller modules and the explicit installed evidence root. Owned preview port 4187 was advanced to `f440f19`, authenticated normally, and the saved annotation persisted. The installed 4180 service and global worker remain unchanged.
- Opus **1578** completed on the first response: all four criteria uphold, 13 comments, native verdict **verified**, actual auth stamp subscription. No correction run was needed. Saved response artifact **631**, SHA-256 `71aa45713921d3b6dd84d4e63863c052c3723eb7056211d928eeb2e4623fe5cb`; criterion notes measure 243/238/245/239 UTF-16 units and the longest comment is 246. This is one observed first-pass success, not a general reliability percentage or guarantee that every provider enforces every schema keyword.
- Root triaged the review's non-blocking notes against source: `parseNumstat` already emits schema/endpoints/fileCount and uses a 400-file cap, and review lines already require positive integers. Annotation **181** limits the next implementation to demonstrated redaction note **167**. The actual result UI sealed all 15 notes into `revise-workspace5-evidence-handoffs-from-15-annotations-`, preserving parent exclusions, paths, criteria and Astra profile. Fresh digest **`69d8ab8d474e62f488556e595a1ee022`** was inspected and approved. Owned-runner build **1579** starts at `f440f19` using that repaired controller, with native steering note 80 clarifying the historical inherited base and limiting work.
- Linked candidate **`284bd26eab8ecb1777637576f7900ae5da140691`** changes three files. Typecheck and 341 focused tests pass (49 context tests plus 292 across six existing suites). Root matched the assessed source/test diff exactly: 17,863 bytes, SHA-256 `476987f419f7acf22f8cb9b08f733afc0e544677ba848cbe052c4d8d88acb19f`. New regressions reproduce missing clean sections and an overly permissive exact-range check, then prove full path-section reselection, multibyte offsets, selected redaction/ambiguous-header rejection and atomic refusal of changed source bytes.
- Root replays with `284bd26` pass parsing and custody without changing live artifacts. Run **1565**: 238,525 encoded bytes / 196,398 content bytes; SHA-256 `f3d732e1ca0f0bfc3e4054a54b7cbc03c9d6c34f1b0f8acc710b6bb3453f2282`. The 3,224-byte store patch now survives the unrelated redaction and prior c1/c2 support is eligible with valid bindings. Some lower-priority sections, including serve, still exceed the aggregate budget, and the genuinely redacted test remains excluded. Run **1572**: 197,540 encoded / 178,134 content bytes; SHA-256 `87e90c13ff3cf8ce82f8b32b00c0861a89ff129cbce802a2dc69b155705666bd`; the status source/revision sections remain available and all prior bindings verify. Changed code correctly prevents automatic prior eligibility. Neither replay rewrites historical judgments or counts as a new independent review.
- Native 1579's unchanged full gate passed: **2,922 passed / 23 existing skips**, 167 suites, 330.75 seconds plus typecheck/build. Check artifact **639**, SHA-256 `142d53f40aa0c85a0a345c0734e8469d993b3e1cf26b4ee8a3718c6cf657f855`. Context artifact **640** is complete schema 2, SHA-256 `73d7926fa1416dfd9e38c41052bf536b7cd27f122000d16c8153f5bc38958ee2`, but has coverage gaps. It was captured by the pinned previous controller `f440f19`, not the code the task was still building.
- Opus **1580** / request 23 again accepted on the first response (nine comments, four judgments), but only gate upheld; integrity/context/handoff were **cannot-tell** because supporting code was missing. Native default-mode verdict is verified from machine checks; this is **not four independent upholds**. Root did not treat that badge as sufficient and did not rewrite the successful historical review.
- The local integration branch fast-forwarded to `284bd26` with the untracked pilot record preserved; `npm run build` refreshed its controller, not a duplicate full suite. A new replay exposed two remaining false classifications: `src/provider.ts` is valid UTF-8 with a literal replacement character, and the context code's quoted marker strings are not actual redacted lines. Root made these small final fixes directly rather than filing another implementation cycle. The new targeted test first failed twice (the malformed-byte rejection passed), then typecheck and **164 tests across three affected existing suites** passed in 22.78 seconds. No tests were deleted or persistently skipped.
- With those direct fixes, run **1579** replay parses and passes custody: 174,098 encoded bytes, SHA-256 `da4a958d3e1be3af5b238cdc427e0f697b20559fb8904e016c3790f1324d3005`. It now supplies the 48,925-byte provider file, 33,753-byte context patch chain, and 5,111-byte reviewer patch. Prior handoff support is eligible. Genuine redacted tests and missing full-file coverage stay gaps. The next naming pilot will use this updated controller and run the unchanged native full gate; direct fixes have not yet been independently reviewed or passed that full gate at this entry.
- Root committed the direct classification fixes and the durable receipt locally as **`bfa520bf3592d6155e1151dc57e7fff5498f8166`**. The existing naming scope was re-inspected unchanged, including its 1400×900/390×844 screenshot requirement, precise three criteria, six paths and Astra/Opus route. Fresh approval used the original proposed digest `e8e40d90f785073f8692e02f16f18413`; native build **1581** starts from `bfa520b` under the updated root controller. No main merge, push or deployment. The obsolete owned preview on 4186 was stopped after checking its PID/cwd; 4187 remains the current preview and installed 4180 is untouched.
- Native naming candidate **`7c813e09cb0b55f2440c7c2c5172cba74e50f615`** passed typecheck/build and the unchanged full gate: **2,932 tests passed / 23 existing skips**, 167 suites, 314.45 seconds. Check artifact **656**, SHA-256 `09df39b33a35269af399375f6d0c49aa5a71fe58ca654da0a07330d0025f06c2`. This gate includes the direct `bfa520b` evidence classification fixes; it is not a new independent review of those fixes.
- Opus **1582** / request 24 accepted all three naming criteria after same-session correction **1583**. All note lengths already fit; the correction moved one comment from the evidence filename `REVIEW-CHECK-LOG.txt` to an actual reviewed path. The unchanged native path check correctly refused the first response. Accepted response artifact **658**, SHA-256 `07e21bda38467ff04eca6053533b438159dea34b647d9c3bbd2e43d304504178`. No extra full gate, cap increase or acceptance bypass.
- Root inspected the named-revision fixture and actual desktop/phone task pages on the candidate console. Names wrap, but task IDs still precede the heading and repeat under navigation titles, contrary to the signed details-only intent. Actual captures: `workspace5-names-provenance-before-{phone,desktop}.png`; the synthetic named-child image is explicitly labeled as such. Root saved ordinary note **191**, line annotation **204**, and bounded triage **205**. Other review questions were checked: the title update is inside the route's outer transaction, the 800-byte cap cannot be exceeded by a 200-UTF-16-unit title, and 1400×900 is explicitly signed.
- The actual result UI sealed this 15-note mixed batch into **`revise-give-revisions-recognizable-names-from-15-annotat`**, displayed as **Give revisions recognizable names — revision**. Exact inherited goal prefix, exclusions, paths, criteria and Astra profile match; repair text correctly says feedback. Source run **1581**, immutable brief artifact **659**, SHA-256 `0937aebf4a4b178ea4d6b4be500dc44917f001dfcfc30924591ba0317049a26e`. Fresh approval **`8eef9330e130aaef4c724cd7a8ba5f54`** precedes native Astra build **1584**, based on `7c813e0` with the repaired `bfa520b` controller. Root integration fast-forwarded to `7c813e0`; main, installed service and global worker remain unchanged.
- Linked candidate **`14e65755258f0f262542d7e4541718090983c962`** changes four files, with only a small presentation edit. Builder reports typecheck, 320 affected tests and 110 browser assertions passing. The browser first caught the existing 36px Task options target; the final CSS makes it 44px. Root inspected its named, long-name, expanded-identity and damaged-evidence fixture images. The unchanged full gate is running at this entry.
- Root previewed this exact candidate against real data on owned port 4187. At 1400px the document fits (1389px client/scroll), sidebar links contain only titles, and the readable name leads. At 390px the newly disclosed identity fits 285px and its summary measures 44px, but the **whole page still overflows to 418px / 379px client**: long paths/hashes in the expanded inherited review-batch rows do not wrap. This older detail-state issue is recorded, not disguised as a full mobile pass or expanded into another implementation cycle. Images: `workspace5-names-real-child-after-{phone,desktop}.png` and `workspace5-names-real-child-details-phone.png`. A follow-up should reuse the existing fixture with real-length annotation paths and hashes; no new engine or suite is needed.
- Native **1584** passed the unchanged full gate once: typecheck/build, **2,932 passed / 23 existing skips**, 167 suites, 323.44 seconds. Check artifact **674**, SHA-256 `1838c133e0f817441bfe79b1ae65b5aea99819b0876c8673b4ec7dc1ae174a96`. Complete context artifact **675** is 155,002 bytes, SHA-256 `d613cdd5d7b24ba1865cd8716ee82b79999bcee74a50063da090bc3c2d71ef32`; parsing and custody checks pass. It contains the exact **1581 + 1584** source/revision sections for serve (8,372 bytes), its tests (12,769), and the browser proof (15,739), plus three complete files. Prior-review evidence bindings all verify despite shortened source check output. Full-file coverage remains explicitly partial and changed code correctly prevents automatic prior eligibility. Native Opus review **1585**, request 25, is assessing this actual packet, not a replay or rewritten old review.
- Opus **1585** completed on its first response, actual auth **subscription**: six comments and **names/limits/lineage all uphold**. Each judgment cites restored context (`ctx-1`, `ctx-5`, `ctx-6`) alongside the fresh checks/images. Accepted response artifact **676**, SHA-256 `e1598bbb0b8463029e69c2b81a33a63260f0af7a27adde3df88e91dfe496f0f2`. No correction child, automatic prior uphold or inherited cannot-tell. This is the successful real subsequent handoff promised by the evidence-repair scope, not a product-wide success-rate claim.
- Root fast-forwarded the local integration branch to **`14e6575`**, preserving its two operator-owned assessment edits. The final reviewed result is visible in the candidate console; screenshot `workspace5-native-handoff-reviewed-phone.png`. Review comments leave two non-blocking polish/evidence opportunities: identical sibling revision names are hard to distinguish in navigation, and the native verifier log does not contain the builder's browser-script output (screenshots are sealed; root read the local 110-pass report and measured the real 44px target). Do not present these as secretly fixed or open another automatic repair cycle without a bounded next task.

## Remaining work

- Repair the earlier reproduced large chat-tool-call budget mismatch and make chat failures persist visibly; these pilots did not change chat transport.
- Resolve pilot 2's original signed manual-review requirement through its normal approval path, not a rewritten criterion or passing badge.
- Wrap long paths/hashes in real inherited-feedback rows on phones; distinguish same-title sibling revisions with concise, nontechnical context. Reuse current tests and proof fixtures.
- Consider sealing the existing browser report/log alongside its screenshots so independent reviewers can verify assertions without trusting the builder's prose.
- Merge/push/deploy the candidate only when requested. The installed older worker remains unchanged and cannot be assumed to understand the new evidence format. Physical-device, Windows and signing checks remain outside this pilot.
