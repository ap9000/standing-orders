# Quiet project learning — execution record

Implemented, independently reviewed, and integrated locally on `codex/review-learning`. No deployment or measured learning benefit is claimed. Live nonempty lesson reuse remains unverified; controlled coverage and actual-provider outcomes are distinguished below.

## Scope and admission

- User approved quiet review learning and requested a settings ledger to understand changes and diagnose regressions.
- Committed the research plan and ledger acceptance addendum at **`2a008a0dd63425ad00a368884f359dbd120afd27`**; local branch `codex/review-learning`.
- Native task **`quiet-project-learning-20260914`**, title **Learn from reviews with a quiet settings ledger**, approved digest **`e3ff58c506e5b60354cf0cbb4522e129`**. Five criteria: capture, reuse, ledger, safety, experience. Astra plans/builds/repairs; Opus reviews. No dollar cap, existing idle policy, no new time limit.
- Owned one-off repo-bound runner **`learning-pilot-20260914`**. Normal native CLI filing, routing and credentialed approval, then one tick from the exact base. Not a claimed autonomous chat filing.
- Native builder **1596** started at 01:26:58 UTC September 15. Its worktree is `standing-orders-quiet-project-learning-20260914-f0ec7afb` under the existing worktree pool.
- Controller frozen at the previously verified `af4c204` runtime. Installed database stays v57. Any new storage migration is for disposable development databases only; no installed app/worker changes, export, push or deployment.

## Acceptance to verify

1. An optional review lesson is supported by exact evidence, stays proposed until adopted, and never turns an informational note into requested task work. Invalid optional learning leaves core review strict and completion usable.
2. Adoption, disable/reset and selection produce source-linked ledger events with actor, time, before/after and affected runs. Replay/stale forms cannot duplicate or retarget actions. Reset preserves historical events and exact run snapshots.
3. A later planner/builder/reviewer receives a bounded eligible selection; stale, disabled, tampered or unauthorized lessons are absent. Guidance never changes scope, approval, provider or verification authority. Selection is not proof that the lesson improved quality.
4. Settings remains discoverable without notification setup, project isolation holds before pagination, and result/ledger interactions work at 1400x900 and 390x844 including empty, long and error states. No new primary navigation item.
5. Program improvements remain labelled suggestions in this slice, not invisible runtime changes or cross-project sharing. Migration and older-reader fencing are tested on disposable databases.

Use focused checks during implementation, the native unchanged full verifier once for the candidate, an independent review, and clearly labelled real-provider versus synthetic acceptance. Record actual outcomes below.

## Isolated real-provider acceptance preparation

- A disposable Node-only CSV project was seeded at `61d703f0fb65c69078e77a14dfa004c9399a4955`, outside the product checkout and installed state. It has one ordinary-value test and an intentionally incomplete CSV encoder.
- Planned native journey: Astra fixes quoting under a narrow signed scope; Opus reviews and may suggest an evidence-backed lesson; explicitly save/enable a useful valid lesson; Astra builds a separate table encoder using the prior row encoder. Inspect the exact later-run snapshot, then disable/reset and inspect retained history.
- Runtime, database, evidence, worker pool and ephemeral credentials are isolated. At preparation no provider had run; actual outcomes are recorded below. A reviewer returning no useful lesson is an honest outcome, not permission to fabricate one or claim benefit.

## Candidate and independent checks in progress

- Builder 1596 committed candidate `0abc9ccab24887fc18a6eceffabda262296bd990`; its native full verification subsequently failed as recorded below. Builder reports 817 affected tests, a later 13-test learning check, typecheck, build and 32 desktop/phone assertions. Root inspected the empty, adoption and ledger screenshots; no observed overflow in those captures. Counts overlap and are not summed.
- Actual isolated Astra run 1 fixed the CSV defect at `1eac8afba47a5f61e292a4f1a128f0bd615fffab`, completed its native gate, and Opus run 2 reviewed it successfully (two comments, one upheld criterion). It emitted **no lesson**. This proves a real optional-empty review, not nonempty reuse or benefit.
- Root independently reproduced a recovery defect at `0abc9cc`: fail the optional capture write for one valid lesson, complete 50 newer reviews without learning, restore storage, then retry recovery three times. Core review remains `no-change`, but the original lesson is never recovered (`captureRows=0`). Repro: operator-owned `output/playwright/learning-recovery-audit.mjs`; disposable store only. A bounded fair recovery/empty-result bookkeeping fix is required before sign-off.

## First gate and native repair

- The unchanged native gate for 1596 ran once: **4 failed, 2,951 passed, 23 existing skips, 169 files, 334.77 seconds**. Three tests expected v57 (`migration-v53-process-custody`, `recipe-creator`, `task-control`); `project-access-ledger` still expected project-scoped Settings to be inaccessible. Typecheck passed; the final build command was not reached after the failed test command. A `built` run means a code candidate exists, not that its evidence is verified.
- Independent Opus review **1597** completed: 12 comments, five judgments, **refuted**. Four problem comments concern the failed gate/inconsistent Settings expectation and outdated pre-gate assessment; other notes remain informational and were not automatically converted into new feature work.
- Standing Orders itself created the unapproved repair **`quiet-project-learning-20260914-fix-1`**. Root reused it rather than filing a duplicate. Added five authenticated, idempotent steering notes covering the independently reproduced recovery defect, the no-scope planner gap, coherent optional review JSON, four gate failures, and lean verification.
- Approved repair digest **`d73f417eafe8e8c7ec90d0bfbeb15ea5`** retains all five original criteria, adds only the four affected test files to the allowed paths, and pins Astra/Opus. No test/approval/evidence waiver or new time limit.
- An initial tick correctly refused the old pre-feature base because it did not contain the source candidate. Retried with the exact source `0abc9cc`; native builder **1598** started **02:13:12 UTC** in `standing-orders-quiet-project-learning-20260914-fix-1-1439aad7`. No duplicate provider run was created by the refusal.

## Repair candidate and live pilot

- Native controller committed the repair as **`c9da48ddbe350fc7b14d5fa1c6045e68239b9d43`** at 02:27:52 UTC. The builder assessment's “uncommitted” wording describes its pre-controller handoff, not the final repository state. It records typecheck, 397 non-overlapping focused tests, the repaired recovery audit, and byte-identical UI/source screenshot inheritance. Root inspected the repair diff; no tests or approval checks were waived.
- Actual pilot Astra builder **3** completed task `learning-real-2` at **`f181a405e1abf8fd36a7d9724ce6dd57b8d762dd`**; its native gate passed. Opus reviewer **4**, using the repaired optional-learning prompt, returned **verified**, two comments and one judgment. It also emitted **no lesson**. Both builders used `gpt-6-astra`, both reviewers `opus`, and all four run records report **subscription** authentication.
- These two real-provider journeys prove normal completion with optional-empty learning and exact empty run-context records, not live nonempty adoption/reuse. Nonempty capture, adoption, genuinely unscoped planner input, later builder/reviewer input, and disabled/stale exclusion are covered by controlled fixtures. No third provider task was filed just to chase a nonempty response.
- Root opened the repaired runtime against the disposable pilot database, enabled reuse through Settings, then used the disclosed **Reset learning** action. The project returned to off, events retained the actor and before/after states, and all four historical snapshots remained present. No installed setting changed.
- Root captured and inspected actual viewport images at 390×844 (`output/playwright/learning-real-reset-phone.png`) and 1400×900 (`output/playwright/learning-real-ledger-desktop.png`). Controls fit, labels remain intact, and ledger detail stays collapsed. These images show the real pilot's empty lessons and retained history, not a synthetic nonempty lesson. Physical phone/Safari behavior is unverified.
- Root independently reran the 51-review failure/recovery audit against committed **`c9da48d`**: `lessonRecovered=true`, `captureRows=51`, `coreOutcome=no-change`, `failureRetained=true`. DOM inspection found no horizontal document overflow at either viewport; desktop action/disclosure heights were 44px. The disposable pilot runner was retired after all four provider runs completed.

## Final native verification

- Run **1598** finished at **02:33:38 UTC**. Its unchanged full command ran once: `npm run typecheck && npm test -- --run --reporter=dot --no-file-parallelism && npm run build`. **Passed:** typecheck, **2,956 tests / 169 files**, and build. **23 existing skips** remain. Test duration was **343.66 seconds**. Evidence: installed evidence store `1598/check-log.txt`, candidate **`c9da48ddbe350fc7b14d5fa1c6045e68239b9d43`**.
- The same tick observed the pre-existing `nightly-deps` routine and refused it as not approved because its old terms lack a signed acceptance criterion. No routine was reapproved or dispatched, and it is outside this feature's changes.
- Independent subscription-authenticated Opus review **1599**, request **30**, ran **02:33:53–02:39:29 UTC** through the frozen schema-v57 controller, bound to repair run 1598: **verified**, all five criteria upheld, 12 comments (10 notes, two questions), no problem comments.
- Root checked both questions. Scoped Settings access was expressly authorized in the approved repair brief; global administration and foreign-project denials remain tested. Empty capture bookkeeping is source-keyed: normal admission and the `one_successful_root_review_per_source` unique index forbid another successful root review for that source. A future product change permitting multiple accepted reviews would need to revise this learning identity; no retry bypass was added here.
- Informational limitations remain recorded: process-local recovery cursors cause duplicate reads across Store instances; legacy no-snapshot rows are revisited in bounded sweeps; the 32-assertion report is not itself sealed, although the eight PNGs are sealed and source UI identity is verified. The planner-specific fixture does not alone prove builder path exclusion; separate learning tests cover applicability. No informational note was silently converted into extra task scope.

## Handoff

- Fast-forwarded the root **feature branch** to reviewed code **`c9da48ddbe350fc7b14d5fa1c6045e68239b9d43`**. The following receipt-only commit does not change runtime code; all runtime verification above binds to that exact candidate.
- Entry point after an authorized update: **Settings → Learning**. Review suggestions are quiet; adoption and enabling reuse are explicit. The ledger records proposed/saved/disabled/reset/context/failure events with source links and retained exact run context. System improvements are suggestions only.
- Both owned one-off runners were retired after their work completed. The installed app, worker and real database remain on schema **57**. No main merge, push, deployment, npm publication, signing, global permission change, or private export was performed. Candidate schema **58** was exercised only in disposable databases; installation requires the existing controlled update/migration path.
- Remaining product work: live nonempty review-to-later-task reuse, then matched enabled/disabled outcome comparisons and a separately authorized system-improvement task. No autonomous quality-gain claim is warranted by these tests or this pilot.
