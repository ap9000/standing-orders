# Package 4 release assessment — September 14, 2026

**No concrete release blocker found in the bounded source review. Proceed to the native final verification gate; release verification is still pending.** Physical iPhone testing is unavailable and remains explicitly unverified. This assessment does not waive the recorded device acceptance gates.

Reviewed candidate `2d54e48647fce6a373ab469197d3ea5ceaffc24a`, runtime commit `23be6e048b324a20fd05af1c52d76acf17db1b57`, against baseline `235eb9188c4a7e4f5df0d3023ecf47d712ce4797`. HEAD matched the candidate on `standing-orders/workspace-4-release-validation-20260914`. The candidate's changes after the runtime commit are documentation only; a current-tree comparison also found no tracked non-documentation differences from that runtime commit.

Read [AGENTS.md](../../AGENTS.md), the [implementation result](WORKSPACE_4_MOBILE_RESULT_2026-09-14.md), and the [research brief](WORKSPACE_4_RESEARCH_2026-09-14.md). Independently inspected all changed runtime and test hunks: `serve`, `chat-continuity`, `mobile-viewport`, and `style-asset`, with relevant surrounding code. This is a bounded release review, not a product-wide audit.

- **Delivery:** the new public CSS route accepts GET/HEAD only at the content-addressed path, after host and URL-token guards. Its input is static `STYLE + RECIPE_CSS`; gzip, representation-specific ETags, conditional responses and immutable caching stay in that helper. HTML/fragments retain no-store responses and existing script nonces. The CSP adds same-origin styles. No approval handler, dependency, build configuration or migration changed in this package.
- **Interaction:** the drawer adds focus entry, Tab wrapping, Escape/close return and cleanup of background inert state. The viewport script uses focused editing plus viewport shrink, with a scale guard, and measures composer height. It reads no field values, sends no requests and captures no keyboard events. These source checks do not establish physical keyboard/browser behavior.
- **Consent and simplicity:** repeated chat setup/status copy and approval counts/jump links are removed. The approval introduction becomes “Review plan” and “Nothing starts until you approve”; full scope, exclusions, paths, acceptance wording, lineage, agent/runtime terms and password confirmation remain. Error and uncertain-receipt messages remain. The phone header loses its nested frame, project cards become rows, and the result loses an extra border. The source supports these reductions; the reported 90px → 57px header and visual quality are inherited measurements.
- **Tests:** stylesheet assertions now fetch the linked asset, retaining negative assertions and focus, motion and token checks. New coverage addresses CSS delivery, viewport shrink and drawer cleanup. No test deletion or new skip was found in the bounded diff.

**Inherited synthetic evidence, not rerun or independently witnessed in this attempt:** the implementation result records typecheck/build success, 318 passing focused tests across the four affected test files, then a separate final drawer-test pass and another typecheck. It also discloses earlier assertion failures and their adaptations. The 318 total is not a fresh full-suite result.

That same record reports desktop and phone result → Changes → feedback → revision journeys with approval retained; fresh empty chats at 1280×800 and 1440×900; and empty, long-approval, damaged-result, Changes and project-dialog states at 320×740, 390×844 and 430×932. It reports zero document overflow across 18 phone states, keyboard/focus recovery, offline draft preservation, settled long-composer clearance, light/dark failures, reduced motion and CSS-zoom/reflow checks. Fixtures used in-memory tasks, scripted replies and disposable repositories on headed Chromium, not live subscriptions or real tasks. Approval views were recaptured after the last copy/CSS reduction; unchanged journeys were reused.

The referenced ignored `output/playwright/` directory, including screenshots, recordings and raw logs, is absent from this worktree. Only the committed narrative is available here. No image was independently inspected or submitted as new screenshot proof. This explicitly limits confidence in visual, timing and transfer claims; the recorded 34,864-byte gzip CSS and warm-cache savings are inherited measurements, not reproduced results.

Current read-only checks passed: `git diff --exit-code 23be6e048b324a20fd05af1c52d76acf17db1b57 -- . ':(exclude)docs/**'` (tracked runtime/test/configuration identity) and `git diff --check 235eb9188c4a7e4f5df0d3023ecf47d712ce4797 HEAD` (inherited diff whitespace). No build, focused-test rerun, new browser journey or full repository verification was performed. The native proof records **c1 met** for this assessment and **c2 pending-verification**: only the machine's unchanged approved final command can establish c2 after its report commit. No final exit code or success is claimed.

Physical iPhone Safari soft keyboard/predictive area, browser chrome, safe areas, rotation, foreground/Back and native zoom remain unverified. CSS zoom is not native browser zoom; Android resize behavior is unit-informed only. The real subscription-backed pilot, unfamiliar-user assessment, signed-Mac installation/access persistence and physical Windows acceptance remain separately deferred as recorded in the inherited result. None is certified by this report.

Only this new assessment is a repository deliverable. It remains uncommitted because the attempt rules reserve committing for the machine, despite the scope's request to commit the report. Runtime, tests, configuration and existing documents remain unchanged.
