name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: node ${{ matrix.node }} · ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: ["22", "24"]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm test
        env:
          CI: "true"

  windows-baseline:
    name: windows baseline · node ${{ matrix.node }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        node: ["22", "24"]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      # The Windows gate covers the native service/link path, the scheduler's
      # Never Stuck contract, and the builder's native-shell setup + proof
      # path. POSIX-only fake-provider fixtures stay in the Unix matrix until
      # their executables are made cross-platform.
      - run: npx vitest run src/daemon.test.ts src/link.test.ts src/dispatch.test.ts src/command-shell.test.ts src/child-database.test.ts src/store-contention.test.ts src/task-control.test.ts src/task-control-console.test.ts src/task-control-git.test.ts src/task-control-adversarial.test.ts
        env:
          CI: "true"

  native-windows:
    name: Windows Job Object containment
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npx vitest run src/containment-windows.test.ts
        env:
          SO_EXPECT_NATIVE_CONTAINMENT: "1"
